<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}EAT ME!{{else}}TOO LATE!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE ME!</button>
</li>
